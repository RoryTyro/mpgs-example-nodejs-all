{
  "name": "MPGS NodeJS example",
  "description": "A basic example of MPGS features in NodeJS for Tyro merchants",
  "repository": "https://github.com/RoryTyro/mpgs-example-nodejs-all",
  "logo": "https://2n18ua1tyz6r3kfqtckmdy45-wpengine.netdna-ssl.com/wp-content/themes/tyro/assets/img/logo.png",
  "keywords": ["node", "MPGS", "Tyro"],
  "env": {
    "GATEWAY_MERCHANT_ID": {
      "required": true,
      "description": "Your TEST Gateway merchant ID (must begin with 'TEST')"
    },
    "GATEWAY_API_PASSWORD": {
      "required": true,
      "description": "Your TEST Gateway API password"
    },
    "GATEWAY_BASE_URL": {
      "required": true,
      "description": "The Gateway base URL to target. Use your region specific url if you have one.", 
    },
    "GATEWAY_API_VERSION": {
      "required": false,
      "description": "Your TEST Gateway API Version"
    },
    "GATEWAY_DEFAULT_CURRENCY": {
      "required": false,
      "description": "Your TEST Gateway Default Currency"
    },
    "WEBHOOKS_NOTIFICATION_SECRET": {
      "required": false,
      "description": "Webhook notification secret for payload validation"
    }
  }
}